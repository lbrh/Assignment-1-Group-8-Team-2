import { describe, it, expect } from 'vitest'

// Placeholder so `pnpm test` and the CI pipeline have something to run.
// Replace with real tests once the frontend features are rebuilt — delete
// this file once real coverage exists, don't leave it alongside real tests.
describe('placeholder', () => {
  it('passes, so CI has a non-empty test suite to gate on', () => {
    expect(true).toBe(true)
  })
})